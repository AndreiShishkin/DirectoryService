services:
  postgres:
    container_name: directory_service_db
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: directory_service_db
      POSTGRES_USER: directoryService
      POSTGRES_PASSWORD: 123456qazwsx
    ports:
      - "6432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql
        
volumes:
  postgres-data: